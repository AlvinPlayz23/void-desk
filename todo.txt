whenevre there is a file edit by agent the epxlorer freaks out and goes to iringal state, and while file being edited if we open the same file it takes 5sec to open




the attachment code i think iss like a bit too much..workarounds, clean it but functional..use hunter alpha true model


d&d still not working
@ fixed but when message sent there is no ui showing that context is added...
started and complete tool indicator 
improve ui for autocomplete(if possible only) and hover
give context to tab iniline compilition..

give context to session thingy

feature log for the new settings page..

IMP----
revert the things done in feature-logs/2026-01-29.md
-----



codex-sugestions-8/2/26:
- Add a real patch tool: apply_patch(path, patch) (unified diff or Zed‑style patch) and prefer it for edits over edit_file when possible.
  - Make streaming_edit_file truly “streaming”: accept edits incrementally (or allow multiple tool calls) and apply/reveal each edit as it resolves; keep the diff UI updated per edit.
  - Improve fuzzy matching: scoring + best‑match selection (not just whitespace normalization), plus better “ambiguous matches” diagnostics (show candidate line numbers).
  - Add read_file previews + caps: optional max_chars or max_lines, and a read_file_metadata tool (size, line count, mtime) to avoid huge reads.
  - Add list_directory recursion + filters: recursive, max_entries, glob, ignore (helps the agent discover projects faster).
  - Add tool permissions UX: when a sensitive path is blocked, return a structured error payload that the UI can render as “Confirm to proceed”, then retry with allow_sensitive=true.
  - Add tests for tools (Rust): unit tests for resolve_and_validate_path, sensitive path detection, fuzzy matching, overlap detection, CRLF handling.
  - Clean up compile warnings later (optional): unused exports/imports in src-tauri/src/sdk/mod.rs and src-tauri/src/sdk/stream/mod.rs.
- Tool call batching: allow the model to propose multiple tool ops, then execute sequentially with progress + rollback hints
 - add retry timeout feature
- recent projects list, reopen last session, restore tabs/layout (might be completed soon)
--[x] in theTerminal: multiple tabs, split panes [working, but still secting terminal still neded]



no placeholder or mockups for rewest thingy


Add unit tests for the following cases:  
1. `selectActiveAISettings` returns correct active settings when provider presets are enabled and a preset is selected  
2. `selectActiveAISettings` returns correct active settings when provider presets are disabled  
3. `normalizeAIModels` returns default model when given an empty array or invalid input  
4. `normalizeProviderPresets` returns default preset when given an empty array or invalid input  
5. `setProviderPresets` correctly normalizes and sets selected preset ID